# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: true
cache: packages
addons:
   apt:
    packages:
      - librdf0-dev
      - libv8-dev
      - libjq-dev